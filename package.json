{
  "name": "sensor-csv-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Korean classroom sensor CSV analyzer prepared for GitHub and Vercel deployment.",
  "scripts": {
    "build": "node scripts/build-static.js",
    "preview": "node scripts/serve-static.js",
    "start": "node scripts/serve-static.js",
    "test": "node --test tests/api.test.js tests/analyzer-core.test.js tests/build-static.test.js tests/deployment-structure.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}